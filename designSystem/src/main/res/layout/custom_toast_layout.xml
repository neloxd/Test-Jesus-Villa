<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root_toast"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:theme="@style/Theme.PASE"
    app:cardBackgroundColor="@color/success_color_toast"
    app:strokeColor="@android:color/transparent"
    app:strokeWidth="@dimen/space_0">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:padding="@dimen/space_14">

        <ImageView
            android:id="@+id/iv_toast"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:src="@drawable/ic_check_circle_white" />

        <TextView
            android:id="@+id/tv_toast"
            style="@style/TextAppearance.App.Input.Title.White"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginLeft="@dimen/space_10"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="2"
            tools:text="Soy un Toast" />
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>